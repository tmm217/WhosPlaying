<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      List of all showfollows
    </h1>
  </div>
</div>

<hr>

<div class="row mb-3">
  <div class="col-md-8 offset-md-2">
    <h2>
      Add a new showfollow
    </h2>

    <form action="/insert_showfollow" method="post">
      <div class="form-group">
        <label for="show_id_box">
          Show
        </label>

        <input type="text" id="show_id_box" name="query_show_id" class="form-control">
      </div>

      <div class="form-group">
        <label for="attendee_id_box">
          Attendee
        </label>

        <input type="text" id="attendee_id_box" name="query_attendee_id" class="form-control">
      </div>

      <button class="btn btn-block btn-outline-secondary">
        Create showfollow
      </button>
    </form>
  </div>
</div>

<hr>
<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Show
        </th>

        <th>
          Attendee
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @showfollows.each do |showfollow| %>
      <tr>
        <td>
          <%= showfollow.id %>
        </td>

        <td>
          <%= showfollow.show_id %>
        </td>

        <td>
          <%= showfollow.attendee_id %>
        </td>

        <td>
          <%= time_ago_in_words(showfollow.created_at) %> ago
        </td>
        <td>
          <%= time_ago_in_words(showfollow.updated_at) %> ago
        </td>

        <td>
          <a href="/showfollows/<%= showfollow.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>

<hr>
